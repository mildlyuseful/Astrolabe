# Python
__pycache__/
*.pyc
.pytest_cache/
build/
*.egg-info/
nuitka-crash-report.xml

# Virtual environments. `uv sync` creates .venv at the repository root, and
# docs/release_verification.md tells contributors to run it.
.venv/
venv/
env/
.uv-cache/
.tmp/

# Generated when west initializes from firmware/zmk/config/west.yml.
/firmware/zmk/.west/
/firmware/zmk/zmk/
/firmware/zmk/zephyr/
/firmware/zmk/modules/
/firmware/zmk/tools/
/firmware/zmk/bootloader/

# .NET build outputs (plugin_src is source; the shipping DLL is bundled under
# trackball_daemon/plugins/ and IS committed)
plugin_src/**/bin/
plugin_src/**/obj/

# Per-user Claude Code settings (personal permissions, not project config)
.claude/settings.local.json
